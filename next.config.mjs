/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
        ignoreDuringBuilds: true,
    },
    // output: 'export'
    // output: 'export' for build on
};

export default nextConfig;
